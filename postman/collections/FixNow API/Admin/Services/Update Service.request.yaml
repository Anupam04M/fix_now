$kind: http-request
url: "{{base_url}}/admin/services/{{service_id}}"
method: PATCH
headers:
  Authorization: Bearer {{admin_token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "base_price": 599
    }
order: 4000
