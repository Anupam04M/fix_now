$kind: http-request
url: "{{base_url}}/provider/assignments/{{assignment_id}}/in-progress"
method: PATCH
headers:
  Authorization: Bearer {{provider_token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "notes": "Service started"
    }
order: 7000
