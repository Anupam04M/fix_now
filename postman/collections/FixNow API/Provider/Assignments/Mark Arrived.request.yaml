$kind: http-request
url: "{{base_url}}/provider/assignments/{{assignment_id}}/arrived"
method: PATCH
headers:
  Authorization: Bearer {{provider_token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "notes": "Arrived at customer location"
    }
order: 6000
