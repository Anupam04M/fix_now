$kind: http-request
url: "{{base_url}}/provider/assignments/{{assignment_id}}/confirm-cod-payment"
method: PATCH
headers:
  Authorization: Bearer {{provider_token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "notes": "Cash collected"
    }
order: 9000
